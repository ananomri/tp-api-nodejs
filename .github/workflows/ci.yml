on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  # ← Ajouter ceci
  workflow_dispatch:
    inputs:
      raison:
        description: 'Raison du lancement manuel'
        required: false
        default: 'Test manuel'